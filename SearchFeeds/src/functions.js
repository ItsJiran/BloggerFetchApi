// +===============================================================+
// ------------------- BLOG SEARCH FUNCTIONS -----------------------
// +===============================================================+
function preg_match_all(regex, str) {
    return regex.exec(str);
}